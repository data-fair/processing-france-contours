{
  "type": "object",
  "x-display": "tabs",
  "allOf": [{
    "title": "Paramètres",
    "properties": {
      "clearFiles": {
        "title": "supprimer les fichiers téléchargés",
        "type": "boolean",
        "default": true
      },
      "simplifyLevel": {
        "title": "niveau de simplification",
        "type": "string",
        "default": "medium",
        "enum": ["full", "precise", "medium", "simple"]
      },
      "datasetIdPrefix": {
        "title": "Préfixe des identifiants de jeux de données",
        "type": "string",
        "default": "france-contours"
      }
    }
  }]
}
