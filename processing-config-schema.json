{
  "type": "object",
  "x-display": "tabs",
  "required": ["years"],
  "allOf": [{
    "title": "Paramètres",
    "properties": {
      "clearFiles": {
        "title": "supprimer les fichiers téléchargés",
        "type": "boolean",
        "default": true
      },
      "skipUpload": {
        "title": "ne pas charger les jeux de données",
        "type": "boolean",
        "default": false
      },
      "simplifyLevel": {
        "title": "niveau de simplification",
        "type": "string",
        "enum": ["full", "precise", "medium", "simple"]
      },
      "datasetIdPrefix": "france-contours"
    }
  }]
}
